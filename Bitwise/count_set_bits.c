#include <stdio.h>
#include <stdint.h>

uint32_t swap_odd_even_bits(uint32_t num, uint8_t bits)
{
    uint32_t even_mask = 0, odd_mask = 0;
    for(uint8_t i = 0; i < bits; i++)
    {
        
    }
}

int main(void)
{
    
}